# 🎙️ 小鲁宙（Podcast Hacker）

> 让创作者 **只需要说话**，  
> 就能把真实表达，  
> 自动整理成一整期 **可发布播客作品** 的工具。


## 🔐 本地环境变量配置（OPENAI_API_KEY）

⚠️ **重要说明**  
开发者需要在 **自己的本地环境** 单独配置 API Key。

### 本地配置步骤（Mac / Linux）

#### 1️⃣ 在项目根目录创建 `.env.local`

在项目根目录（与 `package.json` 同级）执行：

```bash
touch .env.local
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxx
```


## 一、这个产品解决的核心问题

这不是一个「AI 生成播客内容」的工具。

而是一个：

> **帮助不善于表达、容易跑题、害羞的人，  
> 把真实表达转化为可发布播客作品的工具。**

### 核心痛点来自三类真实人群问题

1. **不敢 / 不想找真人对谈**
   - 社交成本高
   - 心理压力大

2. **说话是真实的，但结构是混乱的**
   - 表达跑偏
   - 思路发散

3. **说完之后，不知道怎么整理成「作品」**
   - 只有素材
   - 没有成品

### 破局之处

👉 让 AI 扮演一个 **专业、温和、会深挖的采访者**  
👉 全程只需要 **“说话”**，不需要操作复杂按钮  
👉 最终直接产出一个 **“成熟播客”**，而不是一堆素材  

这是一个：

> 让创作者 **只需要说话**，  
> 就能把真实表达，  
> **自动整理成一整期可发布播客作品** 的工具。

**重要立场：**

- AI 不是主讲者  
- AI 是 **采访者 + 编辑**  
- 内容来源 **始终是用户本人**

---

## 二、整体产品形态：一个「两栏播客创作界面」

### 左侧：真实对谈子界面（Recording / Interview）

这是**实时发生的事情**，高度忠于事实。

#### 功能与体验

- 用户点击一个麦克风按钮：
  - 点击开始录制
  - 再点一次结束
- 用户只需要说话
- AI 会：
  - 像真人一样自然追问
  - 不需要用户点「发送」
  - 对话节奏接近真实访谈

#### 左侧展示内容（非常重要）

- **逐字字幕**
  - 严格按照：
    - 用户真实说出口的内容
    - AI 实际说出口的内容
  - 不润色、不改写、不总结
- 字幕表现为：
  - 实时滚动显示（逐字 / 逐句）
  - 最终沉淀为「逐字稿」

👉 **左侧的定位：**

> **事实层 / 原始记录层**

---

### 右侧：内容整理与结构子界面（Editor / Outline）

这是对**已经发生的真实内容进行整理**，而不是凭空创作。

- 基于最终定稿 turns
- 自动生成并持续更新：
  - 当前播客结构
  - 话题走向
  - 章节划分
  - 金句候选

👉 **右侧的定位：**

> **编辑层 / 结构层 / 创作辅助层**

---

## 三、三种文本层级（忠实性原则）

### 1️⃣ 原始逐字文本（事实层）

- 来自语音转写
- 一字不改
- 是后续所有处理的唯一依据

### 2️⃣ 结构化整理文本（编辑层）

- 忠于原意
- 合并、重排、去跑题
- 不新增事实

### 3️⃣ 润色后的播客脚本（发布层）

- 第一人称
- 非对话体
- 可直接发布

---

## 四、导出的完整成果包

```text
export-YYYY-MM-DD/
├─ audio.mp3
├─ transcript_verbatim.md
├─ script_polished.md
├─ show_notes.md
├─ chapters.txt
└─ quotes.txt
```

## 五、项目总览
```text
podcast-hacker/
├─ app/
│  ├─ page.tsx
│  ├─ api/
│  │  ├─ realtime-session/
│  │  ├─ outline/
│  │  └─ export/
│  └─ components/
│     ├─ InterviewPanel.tsx
│     ├─ OutlinePanel.tsx
│     └─ ExportPanel.tsx
├─ lib/
│  ├─ realtime/
│  ├─ export/
│  └─ ffmpeg/
├─ public/
├─ .env.local
├─ package.json
└─ README.md

三条主线只共享一个核心数据：turns（最终定稿对话）
这保证了系统稳定、可解释、且忠于原意。

```

